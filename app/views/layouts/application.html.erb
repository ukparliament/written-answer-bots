<!DOCTYPE html>
<html>
  <head>
    <title>Written answers</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <style>
    body {font-family: 'Helvetica Neue','Helvetica',sans-serif;padding:1rem;max-width:42rem;line-height:1.4;margin:1rem auto;font-size:1.2rem;}
    a {text-decoration:none;}
    a:hover {text-decoration:underline;}
    h1 {font-weight:normal;}
    </style>
  </head>
  <body>
  
  <header>
  <nav>

	<%= content_tag( 'span', link_to( 'Answers', answer_list_url ) ) %>
	&nbsp;
	<%= content_tag( 'span', link_to( 'Answering bodies', answering_body_list_url ) ) %>
	&nbsp;
	<%= content_tag( 'span', link_to( 'Members', member_list_url ) ) %>

  </nav>
  </header>
  
  <main>
    <%= yield %>
    </main>
    
    <footer>
    </footer>
  </body>
</html>
